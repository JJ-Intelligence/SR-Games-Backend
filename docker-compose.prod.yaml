version: '3.7'

services:
  sr-games-backend:
    container_name: sr-games-backend
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
    - '.:/sr-games-backend'
    ports:
    - '80:8080'
